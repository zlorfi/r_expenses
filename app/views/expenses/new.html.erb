<h2 class="d-none d-md-block"><%= t 'expense.new_expense' %></h2>
<h3 class="d-md-none d-lg-none d-xl-none"><%= t 'expense.new_expense' %></h3>

<%= render 'form', expense: @expense %>

<%= render 'display_table', expenses: Expense.given_organization(current_organization).order(id: :asc).reverse_order.limit(5) %>
